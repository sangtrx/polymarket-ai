---
phase: 01-canonical-artifact-ingestion
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - services/research-gateway/src/ingestion/mod.rs
  - services/research-gateway/src/ingestion/artifact_discovery.rs
  - services/research-gateway/src/ingestion/artifact_parser.rs
  - services/research-gateway/src/ingestion/snapshot_builder.rs
  - services/research-gateway/src/lib.rs
autonomous: true
requirements:
  - ARTF-01
  - ARTF-02
  - ARTF-03
  - ARTF-04
must_haves:
  truths:
    - "User-intent artifacts from PRD, architecture, stories, and roadmap are discovered and parsed into canonical items."
    - "Ingestion accepts only scoped source locations and requirement-bearing structures."
    - "Schema errors fail ingestion while minor quality issues emit warnings."
  artifacts:
    - path: "services/research-gateway/src/ingestion/artifact_discovery.rs"
      provides: "Scoped source discovery for `.planning/`, `docs/`, BMAD-managed directories"
    - path: "services/research-gateway/src/ingestion/artifact_parser.rs"
      provides: "Requirement-bearing extraction from headings, checklists, acceptance criteria tables"
    - path: "services/research-gateway/src/ingestion/snapshot_builder.rs"
      provides: "Canonical item assembly and conflict/equivalence records"
  key_links:
    - from: "artifact_discovery.rs"
      to: "artifact_parser.rs"
      via: "filtered source manifest"
      pattern: "ArtifactSource.*(Prd|Architecture|Story|Roadmap)"
    - from: "artifact_parser.rs"
      to: "snapshot_builder.rs"
      via: "parsed item stream with validation outcomes"
      pattern: "ParsedArtifactItem|ValidationSeverity"
---

<objective>
Implement canonical artifact discovery + parsing + normalization pipeline for PRD, architecture, story, and roadmap sources.

Purpose: Deliver ARTF-01..ARTF-04 with strict scope boundaries and validation semantics from D-04..D-07, D-11, D-12.
Output: Research-gateway ingestion module that produces canonical snapshot input payloads and conflict/warning findings.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-artifact-ingestion/01-CONTEXT.md
@services/research-gateway/src/validation/workflow_runs.rs
@crates/domain/src/audit_artifacts.rs
@crates/persistence/src/postgres/canonical_artifacts.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scoped artifact discovery for BMAD intent sources only</name>
  <read_first>.planning/phases/01-canonical-artifact-ingestion/01-CONTEXT.md, services/research-gateway/src/validation/workflow_runs.rs, services/research-gateway/Cargo.toml</read_first>
  <files>services/research-gateway/src/ingestion/mod.rs, services/research-gateway/src/ingestion/artifact_discovery.rs, services/research-gateway/src/lib.rs</files>
  <behavior>
    - Test: discovery includes only PRD/architecture/story/roadmap files (D-05).
    - Test: discovery roots are restricted to `.planning/`, `docs/`, BMAD-managed dirs (D-06).
    - Test: generated telemetry/log payload excludes non-intent/generated chatter files (D-07).
  </behavior>
  <action>Create `ingestion` module and discovery manifest types; implement deterministic directory walk/filter rules that admit only BMAD intent artifacts per D-05/D-06 and reject telemetry/chatter files per D-07; export ingestion module from `services/research-gateway/src/lib.rs`.</action>
  <verify>
    <automated>cargo test -p research-gateway ingestion::artifact_discovery::tests::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\".planning\"" services/research-gateway/src/ingestion/artifact_discovery.rs`
    - `grep -q "\"docs\"" services/research-gateway/src/ingestion/artifact_discovery.rs`
    - `cargo test -p research-gateway ingestion::artifact_discovery::tests::` exits 0
  </acceptance_criteria>
  <done>Discovery pipeline returns deterministic, scope-limited artifact manifests ready for parser ingestion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse requirement-bearing markdown structures with fail/warn policy</name>
  <read_first>services/research-gateway/src/ingestion/artifact_discovery.rs, crates/domain/src/audit_artifacts.rs, .planning/phases/01-canonical-artifact-ingestion/01-CONTEXT.md</read_first>
  <files>services/research-gateway/src/ingestion/artifact_parser.rs, services/research-gateway/src/ingestion/mod.rs</files>
  <behavior>
    - Test: headings/checklists/acceptance-criteria tables are extracted as canonical candidate items (D-07).
    - Test: structural/schema violations fail ingestion with machine-readable error code (D-04).
    - Test: minor quality issues become warnings and do not hard-fail run (D-04, D-12).
  </behavior>
  <action>Implement parser logic and normalized extraction structs for headings/checklists/acceptance-criteria tables; call domain validators so schema/structural issues fail closed while minor quality issues produce warning findings; include source-specific ID retention metadata for later equivalence mapping (D-02, D-04, D-07, D-12).</action>
  <verify>
    <automated>cargo test -p research-gateway ingestion::artifact_parser::tests::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Acceptance" services/research-gateway/src/ingestion/artifact_parser.rs`
    - `grep -q "ValidationSeverity::Warning" services/research-gateway/src/ingestion/artifact_parser.rs`
    - `cargo test -p research-gateway ingestion::artifact_parser::tests::` exits 0
  </acceptance_criteria>
  <done>Parser emits validated parsed-item stream with deterministic normalization inputs and explicit warning/error outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build snapshot assembly that preserves equivalence links and unresolved conflicts</name>
  <read_first>services/research-gateway/src/ingestion/artifact_parser.rs, crates/persistence/src/postgres/canonical_artifacts.rs, .planning/phases/01-canonical-artifact-ingestion/01-CONTEXT.md</read_first>
  <files>services/research-gateway/src/ingestion/snapshot_builder.rs, services/research-gateway/src/ingestion/mod.rs</files>
  <behavior>
    - Test: source-specific IDs are preserved and linked through explicit equivalence records (D-02).
    - Test: semantic conflicts are persisted as unresolved records without auto-merge (D-11).
    - Test: run remains successful with valid schema while surfacing conflicts in findings (D-12).
  </behavior>
  <action>Implement snapshot builder that converts parsed items into canonical snapshot payloads, creates explicit equivalence links, generates unresolved conflict records, and composes a high-visibility findings set; do not auto-merge conflicting intent; keep successful status when schema is valid (D-02, D-11, D-12).</action>
  <verify>
    <automated>cargo test -p research-gateway ingestion::snapshot_builder::tests::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "equivalence" services/research-gateway/src/ingestion/snapshot_builder.rs`
    - `grep -q "unresolved" services/research-gateway/src/ingestion/snapshot_builder.rs`
    - `cargo test -p research-gateway ingestion::snapshot_builder::tests::` exits 0
  </acceptance_criteria>
  <done>Ingestion module can produce canonical payloads with explicit equivalences/conflicts and validation findings for downstream persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem markdown artifacts → parser pipeline | Untrusted repository content enters extraction/normalization |
| parser output → snapshot builder | Parsed items cross into canonical identity/conflict resolution logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `artifact_parser.rs` | mitigate | Reject schema/structural malformed input with machine-readable error codes and explicit field errors |
| T-01-05 | I | source discovery manifest | mitigate | Restrict source roots to `.planning/`, `docs/`, BMAD-managed paths and filter non-intent artifacts |
| T-01-06 | R | conflict/equivalence pipeline | mitigate | Persist explicit equivalence/conflict evidence for deterministic audit trails; no silent merge |
</threat_model>

<verification>
Run `cargo test -p research-gateway ingestion::artifact_discovery::tests:: ingestion::artifact_parser::tests:: ingestion::snapshot_builder::tests::`.
</verification>

<success_criteria>
Research-gateway ingestion module deterministically parses scoped BMAD artifacts and emits canonical snapshot payloads with explicit warnings/conflicts and no out-of-scope sources.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-artifact-ingestion/01-canonical-artifact-ingestion-02-SUMMARY.md`
</output>

