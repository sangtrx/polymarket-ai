---
phase: 01-canonical-artifact-ingestion
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - crates/persistence/src/postgres/canonical_artifacts.rs
  - crates/persistence/migrations/20260409000200_canonical_snapshot_immutability.sql
  - services/research-gateway/src/ingestion/service.rs
autonomous: true
requirements:
  - ARTF-05
gap_closure: true
must_haves:
  truths:
    - "Snapshot metadata (commit_sha, ingested_at_utc, file_digests_json, aggregate_digest) cannot be overwritten after first persistence."
    - "Re-ingesting identical artifacts at a different ingested_at_utc produces a distinct snapshot identity instead of mutating prior metadata."
    - "Automated tests fail if snapshot immutability or deterministic snapshot identity regress."
  artifacts:
    - path: "crates/persistence/src/postgres/canonical_artifacts.rs"
      provides: "Insert-only snapshot persistence behavior plus regression tests for immutable metadata."
    - path: "crates/persistence/migrations/20260409000200_canonical_snapshot_immutability.sql"
      provides: "Database-level guardrail rejecting metadata updates on canonical snapshots."
    - path: "services/research-gateway/src/ingestion/service.rs"
      provides: "Snapshot identity composition aligned to commit SHA + ingestion timestamp semantics."
  key_links:
    - from: "services/research-gateway/src/ingestion/service.rs"
      to: "crates/persistence/src/postgres/canonical_artifacts.rs"
      via: "snapshot_id composition and snapshot insert call path"
      pattern: "snapshot_id\\(|insert_snapshot\\(|persist_snapshot"
    - from: "crates/persistence/src/postgres/canonical_artifacts.rs"
      to: "crates/persistence/migrations/20260409000200_canonical_snapshot_immutability.sql"
      via: "constraint/trigger rejection path for mutable metadata updates"
      pattern: "immutable|ON CONFLICT|canonical_ingestion_snapshots"
---

<objective>
Close the Phase 01 verification gap by enforcing immutable canonical snapshot metadata and protecting deterministic snapshot identity behavior.

Purpose: Complete ARTF-05 without mutable upsert overwrite behavior and prevent replay ambiguity from snapshot key collisions.
Output: Insert-only snapshot persistence, DB-level immutability safeguards, and regression coverage proving metadata cannot be mutated.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-artifact-ingestion/01-VERIFICATION.md
@.planning/phases/01-canonical-artifact-ingestion/01-REVIEW.md
@.planning/phases/01-canonical-artifact-ingestion/01-canonical-artifact-ingestion-01-SUMMARY.md
@.planning/phases/01-canonical-artifact-ingestion/01-canonical-artifact-ingestion-03-SUMMARY.md
@crates/persistence/src/postgres/canonical_artifacts.rs
@crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql
@services/research-gateway/src/ingestion/service.rs

<interfaces>
From `crates/persistence/src/postgres/canonical_artifacts.rs`:
```rust
pub async fn upsert_snapshot<'e, E>(
    executor: E,
    snapshot: &CanonicalSnapshot,
) -> Result<(), CanonicalArtifactPersistenceError>
where
    E: PgExecutor<'e>;
```

From `services/research-gateway/src/ingestion/service.rs`:
```rust
fn snapshot_id(snapshot: &CanonicalSnapshot) -> Result<String, CanonicalIngestionError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace mutable snapshot upsert with immutable insert-once behavior and regression tests</name>
  <read_first>.planning/phases/01-canonical-artifact-ingestion/01-VERIFICATION.md, crates/persistence/src/postgres/canonical_artifacts.rs, services/research-gateway/src/ingestion/service.rs</read_first>
  <files>crates/persistence/src/postgres/canonical_artifacts.rs, services/research-gateway/src/ingestion/service.rs</files>
  <behavior>
    - Test: existing snapshot metadata cannot be overwritten when the same snapshot key is retried.
    - Test: snapshot identity differs when `ingested_at_utc` differs for same commit/digest content.
    - Test: immutable snapshot conflict returns machine-readable persistence error code.
  </behavior>
  <action>In `canonical_artifacts.rs`, replace `upsert_snapshot` behavior with insert-once semantics (rename to `insert_snapshot` or equivalent) so metadata columns are never updated on conflict; for same `(commit_sha, ingested_at_utc)` collisions, return deterministic error/no-op behavior without changing stored digests (per D-08, D-09). In `service.rs`, update `snapshot_id` composition to include ingestion timestamp context so distinct ingestion times cannot collide on snapshot identity (per D-08). Keep full-snapshot mode behavior unchanged (per D-10).</action>
  <verify>
    <automated>cargo test -p persistence postgres::canonical_artifacts::tests::snapshot_metadata_immutable_ && cargo test -p research-gateway ingestion::service::tests::snapshot_identity_</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "INSERT INTO canonical_ingestion_snapshots" crates/persistence/src/postgres/canonical_artifacts.rs`
    - `grep -q "canonical_artifact_constraint_violation" crates/persistence/src/postgres/canonical_artifacts.rs`
    - `cargo test -p persistence postgres::canonical_artifacts::tests::snapshot_metadata_immutable_` exits 0
    - `cargo test -p research-gateway ingestion::service::tests::snapshot_identity_` exits 0
  </acceptance_criteria>
  <done>Persistence/service layers enforce immutable metadata writes and deterministic snapshot identity across replay scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add database-level immutable metadata guard migration and contract assertions</name>
  <read_first>crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql, crates/persistence/src/postgres/canonical_artifacts.rs, .planning/phases/01-canonical-artifact-ingestion/01-VERIFICATION.md</read_first>
  <files>crates/persistence/migrations/20260409000200_canonical_snapshot_immutability.sql, crates/persistence/src/postgres/canonical_artifacts.rs</files>
  <behavior>
    - Test: metadata update attempts against `canonical_ingestion_snapshots` are rejected at DB level.
    - Test: migration contract tests assert immutable metadata guard exists.
    - Test: insert path remains valid for first-write snapshots.
  </behavior>
  <action>Create follow-up migration `20260409000200_canonical_snapshot_immutability.sql` that enforces immutable snapshot metadata in PostgreSQL (for example with a BEFORE UPDATE trigger rejecting changes to `commit_sha`, `ingested_at_utc`, `file_digests_json`, `aggregate_digest`). Add/update migration contract tests in `canonical_artifacts.rs` to assert this guard exists and aligns with gap closure requirement from 01-VERIFICATION.</action>
  <verify>
    <automated>cargo test -p persistence postgres::canonical_artifacts::tests::migration_contract_snapshot_immutability_</automated>
  </verify>
  <acceptance_criteria>
    - `test -f crates/persistence/migrations/20260409000200_canonical_snapshot_immutability.sql`
    - `grep -q "canonical_ingestion_snapshots" crates/persistence/migrations/20260409000200_canonical_snapshot_immutability.sql`
    - `grep -Eq "TRIGGER|RAISE EXCEPTION" crates/persistence/migrations/20260409000200_canonical_snapshot_immutability.sql`
    - `cargo test -p persistence postgres::canonical_artifacts::tests::migration_contract_snapshot_immutability_` exits 0
  </acceptance_criteria>
  <done>Schema now enforces immutable snapshot metadata independently of application logic, and tests guard against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestion service → postgres snapshot store | Trusted app logic writes to durable snapshot metadata that must remain tamper-resistant post-insert |
| migration constraints → future write paths | Any future adapter/query must be unable to mutate immutable metadata columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | T | `canonical_ingestion_snapshots` writes | mitigate | Replace mutable upsert with insert-only semantics and return typed conflict errors on duplicate immutable keys |
| T-01-11 | R | snapshot audit history | mitigate | Enforce DB-level immutable metadata guard so prior snapshot evidence cannot be rewritten |
| T-01-12 | D | snapshot identity generation | mitigate | Include ingestion timestamp in snapshot identity composition and cover with deterministic tests to avoid collision-driven overwrite behavior |
</threat_model>

<verification>
Run `cargo test -p persistence postgres::canonical_artifacts::tests::snapshot_metadata_immutable_ postgres::canonical_artifacts::tests::migration_contract_snapshot_immutability_` and `cargo test -p research-gateway ingestion::service::tests::snapshot_identity_`.
</verification>

<success_criteria>
Phase 01 verification truth #7 is fully satisfied: snapshot metadata is immutable by behavior and schema, and deterministic replay metadata cannot be overwritten by conflict paths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-artifact-ingestion/01-canonical-artifact-ingestion-04-SUMMARY.md`
</output>
