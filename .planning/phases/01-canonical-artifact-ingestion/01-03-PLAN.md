---
phase: 01-canonical-artifact-ingestion
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - services/research-gateway/src/ingestion/service.rs
  - services/research-gateway/src/ingestion/mod.rs
  - services/research-gateway/src/main.rs
  - tests/api/phase-1-ingestion.test.mjs
  - tests/e2e/phase-1-canonical-ingestion.e2e.test.mjs
  - package.json
autonomous: true
requirements:
  - ARTF-01
  - ARTF-02
  - ARTF-03
  - ARTF-04
  - ARTF-05
must_haves:
  truths:
    - "User can run ingestion and receive snapshot metadata for one canonical dataset."
    - "Re-running full snapshot ingestion for unchanged inputs keeps canonical IDs and digests stable."
    - "PRD, architecture, stories, and roadmap ingestion are all validated by automated tests."
  artifacts:
    - path: "services/research-gateway/src/main.rs"
      provides: "CLI entry to trigger canonical ingestion"
    - path: "tests/api/phase-1-ingestion.test.mjs"
      provides: "Automated contract checks for all required artifact classes"
    - path: "tests/e2e/phase-1-canonical-ingestion.e2e.test.mjs"
      provides: "Determinism check for full-snapshot replay"
  key_links:
    - from: "main.rs"
      to: "ingestion/service.rs"
      via: "CLI command handler invokes ingestion service"
      pattern: "ingest-artifacts|run_canonical_ingestion"
    - from: "phase-1-canonical-ingestion.e2e.test.mjs"
      to: "research-gateway binary output"
      via: "repeat run asserts stable ids/digests"
      pattern: "snapshot_digest|canonical_requirement_id"
---

<objective>
Wire canonical ingestion into an executable CLI flow and add phase-level API/E2E verification for deterministic outputs.

Purpose: Complete user-visible ingestion path and validate ARTF-01..ARTF-05 end-to-end.
Output: `research-gateway` ingestion command + phase-specific API/E2E automated tests + npm script entrypoint.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-artifact-ingestion/01-VALIDATION.md
@services/research-gateway/src/main.rs
@services/research-gateway/src/ingestion/mod.rs
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical ingestion service orchestration and CLI command surface</name>
  <read_first>services/research-gateway/src/main.rs, services/research-gateway/src/ingestion/mod.rs, .planning/phases/01-canonical-artifact-ingestion/01-CONTEXT.md</read_first>
  <files>services/research-gateway/src/ingestion/service.rs, services/research-gateway/src/ingestion/mod.rs, services/research-gateway/src/main.rs</files>
  <behavior>
    - Test: `ingest-artifacts` command ingests PRD/architecture/story/roadmap into one snapshot.
    - Test: output includes commit_sha, ingested_at_utc, snapshot_digest, and counts by artifact type.
    - Test: structural/schema errors exit non-zero with machine-readable error code payload.
  </behavior>
  <action>Create `ingestion/service.rs` orchestrator that calls discovery→parser→snapshot builder→persistence and exposes `run_canonical_ingestion`; update `main.rs` to support `ingest-artifacts --commit-sha <sha> --ingested-at-utc <RFC3339> --repo-root <path>` command; enforce full-snapshot mode only (D-10) and include deterministic metadata per D-08/D-09.</action>
  <verify>
    <automated>cargo test -p research-gateway ingestion::service::tests:: && cargo test -p research-gateway main::tests::ingest_artifacts_command_</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ingest-artifacts" services/research-gateway/src/main.rs`
    - `grep -q "run_canonical_ingestion" services/research-gateway/src/ingestion/service.rs`
    - `cargo test -p research-gateway ingestion::service::tests::` exits 0
  </acceptance_criteria>
  <done>Canonical ingestion is executable through research-gateway CLI with deterministic snapshot metadata and fail-closed errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add phase API and E2E deterministic tests for ARTF-01..ARTF-05</name>
  <read_first>.planning/phases/01-canonical-artifact-ingestion/01-VALIDATION.md, tests/api/story-6-3*.test.mjs, tests/e2e/story-6-3*.test.mjs</read_first>
  <files>tests/api/phase-1-ingestion.test.mjs, tests/e2e/phase-1-canonical-ingestion.e2e.test.mjs</files>
  <behavior>
    - Test: API-level checks confirm PRD/architecture/story/roadmap ingestion each produces canonical items.
    - Test: repeated full snapshot run with same input produces same canonical IDs and aggregate digest.
    - Test: conflict findings are emitted as unresolved records while ingestion still succeeds on valid schema.
  </behavior>
  <action>Create Node tests aligned to 01-VALIDATION mapping: API test validates ARTF-01..ARTF-04 coverage per artifact type, E2E test validates ARTF-05 determinism (stable IDs + snapshot digest) and D-11/D-12 conflict visibility behavior.</action>
  <verify>
    <automated>node --test tests/api/phase-1-ingestion.test.mjs && node --test tests/e2e/phase-1-canonical-ingestion.e2e.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ARTF-01" tests/api/phase-1-ingestion.test.mjs`
    - `grep -q "snapshot_digest" tests/e2e/phase-1-canonical-ingestion.e2e.test.mjs`
    - `node --test tests/api/phase-1-ingestion.test.mjs` exits 0
  </acceptance_criteria>
  <done>Automated tests prove all required artifact classes ingest correctly and deterministic full-snapshot replay holds.</done>
</task>

<task type="auto">
  <name>Task 3: Add phase verification script alias and enforce completion checks</name>
  <read_first>package.json, .planning/phases/01-canonical-artifact-ingestion/01-VALIDATION.md</read_first>
  <files>package.json</files>
  <action>Add script `qa:test:phase-1` with exact command chain `cargo test -p research-gateway ingestion:: && node --test tests/api/phase-1-ingestion.test.mjs tests/e2e/phase-1-canonical-ingestion.e2e.test.mjs`; keep runtime under 180 seconds target from 01-VALIDATION and use it as plan verification gate.</action>
  <verify>
    <automated>node --test tests/api/phase-1-ingestion.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\"qa:test:phase-1\"" package.json`
    - `node --test tests/api/phase-1-ingestion.test.mjs` exits 0
  </acceptance_criteria>
  <done>Phase-specific command exists and executes all canonical ingestion checks in one deterministic gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args/user invocation → ingestion service | User-provided command inputs drive ingestion and persistence behavior |
| ingestion service → test/readiness signal | Generated snapshot evidence drives downstream deployment-readiness auditing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | S | `main.rs` CLI entry | mitigate | Validate required command args (`commit-sha`, RFC3339 timestamp, repo root) and reject malformed values with typed errors |
| T-01-08 | D | ingestion command execution | mitigate | Restrict to full-snapshot single-run path and enforce bounded file scan roots/filters from discovery module |
| T-01-09 | R | snapshot audit evidence | mitigate | Emit deterministic snapshot metadata + digest fields and assert replay stability in E2E tests |
</threat_model>

<verification>
Run `npm run qa:test:phase-1` and confirm both Rust ingestion tests and Node phase tests pass.
</verification>

<success_criteria>
Phase 1 exposes a runnable ingestion command and deterministic automated validation that proves ARTF-01..ARTF-05 behaviors end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-artifact-ingestion/01-canonical-artifact-ingestion-03-SUMMARY.md`
</output>
