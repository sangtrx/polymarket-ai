---
phase: 01-canonical-artifact-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/domain/src/audit_artifacts.rs
  - crates/domain/src/lib.rs
  - crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql
  - crates/persistence/src/postgres/canonical_artifacts.rs
  - crates/persistence/src/postgres/mod.rs
autonomous: true
requirements:
  - ARTF-05
must_haves:
  truths:
    - "Canonical requirement IDs are deterministic from artifact path + heading slug + item index."
    - "Snapshot metadata is immutable and includes commit SHA, ingested_at timestamp, per-file digest, and aggregate digest."
  artifacts:
    - path: "crates/domain/src/audit_artifacts.rs"
      provides: "Canonical artifact ID and snapshot contract validators"
    - path: "crates/persistence/src/postgres/canonical_artifacts.rs"
      provides: "Postgres persistence adapter for snapshots, items, equivalences, conflicts"
    - path: "crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql"
      provides: "Schema constraints enforcing immutable snapshot and canonical IDs"
  key_links:
    - from: "crates/domain/src/audit_artifacts.rs"
      to: "crates/persistence/src/postgres/canonical_artifacts.rs"
      via: "shared canonical structs and reason-code-safe validation"
      pattern: "CanonicalArtifact|CanonicalSnapshot|canonical_requirement_id"
---

<objective>
Create typed canonical ingestion contracts and persistence foundation required for deterministic snapshot storage.

Purpose: Enforce D-01, D-03, D-08, D-09, D-10 invariants before parser/orchestrator code is added.
Output: Domain contract module + migration + Postgres adapter for canonical snapshots/items/equivalences/conflicts.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-artifact-ingestion/01-CONTEXT.md
@.planning/phases/01-canonical-artifact-ingestion/01-RESEARCH.md
@crates/domain/src/research.rs
@crates/persistence/src/postgres/validation_runs.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical artifact domain contracts with deterministic ID + snapshot validators</name>
  <read_first>crates/domain/src/research.rs, crates/domain/src/lib.rs, .planning/phases/01-canonical-artifact-ingestion/01-CONTEXT.md</read_first>
  <files>crates/domain/src/audit_artifacts.rs, crates/domain/src/lib.rs</files>
  <behavior>
    - Test: same artifact path + heading slug + item index always returns the same canonical ID.
    - Test: structural/schema-invalid payload returns fail-closed contract error (D-04).
    - Test: minor quality issue returns warning classification without failing snapshot construction (D-04, D-12).
  </behavior>
  <action>Create `audit_artifacts.rs` with canonical structs/enums/errors and validator/normalizer functions implementing D-01/D-03/D-04/D-08/D-09; canonical IDs MUST follow `artifact.section.item`; ID anchors MUST use `artifact path + heading slug + item index`; export module in `crates/domain/src/lib.rs`.</action>
  <verify>
    <automated>cargo test -p domain audit_artifacts::tests::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "artifact.section.item" crates/domain/src/audit_artifacts.rs`
    - `grep -q "path.*heading_slug.*item_index" crates/domain/src/audit_artifacts.rs`
    - `cargo test -p domain audit_artifacts::tests::` exits 0
  </acceptance_criteria>
  <done>Domain module compiles, exports canonical contract types, and test coverage proves deterministic IDs plus fail/warn validation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical ingestion schema migration with immutable snapshot constraints</name>
  <read_first>crates/persistence/migrations/20260407193000_validation_runs_validation_artifacts.sql, .planning/phases/01-canonical-artifact-ingestion/01-CONTEXT.md</read_first>
  <files>crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql</files>
  <behavior>
    - Test: snapshot row key is unique for commit_sha + ingested_at (D-08).
    - Test: item rows require canonical ID format and artifact type guardrails (D-01, D-05).
    - Test: conflict rows store unresolved status without auto-merge semantics (D-11).
  </behavior>
  <action>Create migration tables for `canonical_ingestion_snapshots`, `canonical_artifact_items`, `canonical_item_equivalences`, and `canonical_item_conflicts`; enforce immutable snapshot keys (commit_sha + ingested_at), per-file digest fields + aggregate digest, and unresolved conflict records per D-08/D-09/D-11/D-12.</action>
  <verify>
    <automated>cargo test -p persistence postgres::canonical_artifacts::tests::migration_contract_</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "canonical_ingestion_snapshots" crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql`
    - `grep -q "UNIQUE (commit_sha, ingested_at_utc)" crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql`
    - `grep -q "canonical_item_conflicts" crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql`
  </acceptance_criteria>
  <done>Migration introduces deterministic snapshot + canonical item storage with schema constraints aligned to D-08..D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Postgres canonical artifact repository adapter</name>
  <read_first>crates/persistence/src/postgres/validation_runs.rs, crates/persistence/src/postgres/mod.rs, crates/domain/src/audit_artifacts.rs</read_first>
  <files>crates/persistence/src/postgres/canonical_artifacts.rs, crates/persistence/src/postgres/mod.rs</files>
  <behavior>
    - Test: upsert/list snapshot returns deterministic ordering for replay safety.
    - Test: conflict and equivalence records persist independently (D-02, D-11).
    - Test: DB errors map to machine-readable typed persistence errors.
  </behavior>
  <action>Add `canonical_artifacts.rs` with typed CRUD functions and explicit ORDER BY clauses for deterministic reads (D-09, D-10); persist source-specific IDs and equivalence links (D-02); persist unresolved conflicts without merge behavior (D-11); export module via `postgres/mod.rs`.</action>
  <verify>
    <automated>cargo test -p persistence postgres::canonical_artifacts::tests::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pub mod canonical_artifacts;" crates/persistence/src/postgres/mod.rs`
    - `grep -q "ORDER BY" crates/persistence/src/postgres/canonical_artifacts.rs`
    - `cargo test -p persistence postgres::canonical_artifacts::tests::` exits 0
  </acceptance_criteria>
  <done>Persistence adapter is available to service layer with deterministic ordering, typed errors, and explicit equivalence/conflict support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown artifact input → domain contracts | Untrusted document content crosses into canonical model creation |
| domain contracts → postgres adapter | Validated data crosses into persistent storage constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `crates/domain/src/audit_artifacts.rs` | mitigate | Fail-on-structural/schema validation with typed contract errors per D-04 before persistence |
| T-01-02 | R | snapshot persistence | mitigate | Store immutable commit_sha + ingested_at + per-file/aggregate digests (D-08, D-09) |
| T-01-03 | E | canonical artifact tables | mitigate | Enforce DB constraints and typed persistence error mapping for malformed/unauthorized mutations |
</threat_model>

<verification>
Run `cargo test -p domain audit_artifacts::tests::` and `cargo test -p persistence postgres::canonical_artifacts::tests::` after implementation.
</verification>

<success_criteria>
Canonical contracts and persistence schema exist, compile, and enforce deterministic ID + immutable snapshot invariants required for ARTF-05.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-artifact-ingestion/01-canonical-artifact-ingestion-01-SUMMARY.md`
</output>

