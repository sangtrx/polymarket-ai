---
phase: 02-evidence-traceability-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/domain/src/traceability.rs
  - crates/domain/src/lib.rs
  - crates/persistence/migrations/20260409000300_traceability_mapping.sql
  - crates/persistence/src/postgres/traceability.rs
  - crates/persistence/src/postgres/mod.rs
autonomous: true
requirements: [TRAC-01, TRAC-03]
must_haves:
  truths:
    - "Every traceability link is keyed by canonical requirement ID and can store one-or-many code/test evidence anchors."
    - "Every persisted link includes non-empty rationale and deterministic confidence level."
    - "Ambiguous, missing, and stale evidence outcomes are represented explicitly instead of silently dropped."
  artifacts:
    - path: "crates/domain/src/traceability.rs"
      provides: "Traceability contracts and validation rules for anchors/rationale/confidence/outcomes"
      min_lines: 120
    - path: "crates/persistence/migrations/20260409000300_traceability_mapping.sql"
      provides: "Traceability schema and constraints for many-to-many evidence links"
      contains: "traceability_links"
    - path: "crates/persistence/src/postgres/traceability.rs"
      provides: "SQLx adapter with typed machine-readable persistence errors and deterministic reads"
      exports: ["insert_traceability_snapshot", "list_links_by_requirement"]
  key_links:
    - from: "crates/domain/src/traceability.rs"
      to: "crates/persistence/src/postgres/traceability.rs"
      via: "validated TraceabilityLink structs persisted through sqlx binds"
      pattern: "TraceabilityLink"
    - from: "crates/persistence/src/postgres/traceability.rs"
      to: "crates/persistence/migrations/20260409000300_traceability_mapping.sql"
      via: "query constants referencing traceability tables"
      pattern: "traceability_"
---

<objective>
Establish the Phase 2 contract and persistence foundation for requirement-to-evidence mapping.

Purpose: Ensure traceability data is deterministic, explicit, and auditable before orchestration is added.
Output: Domain link contracts, migration constraints, and Postgres adapter APIs for traceability mappings.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-evidence-traceability-mapping/02-CONTEXT.md
@.planning/phases/02-evidence-traceability-mapping/02-RESEARCH.md
@crates/domain/src/audit_artifacts.rs
@crates/persistence/src/postgres/canonical_artifacts.rs

<interfaces>
From `crates/domain/src/audit_artifacts.rs`:
```rust
pub fn canonical_requirement_id(
    artifact_path: &str,
    heading_slug: &str,
    item_index: u32,
) -> Result<String, CanonicalArtifactContractError>;
```

From `crates/persistence/src/postgres/canonical_artifacts.rs`:
```rust
pub struct CanonicalArtifactPersistenceError {
    pub code: &'static str,
    pub message: String,
}
pub async fn insert_snapshot<'e, E>(...) -> Result<String, CanonicalArtifactPersistenceError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define traceability contracts and validation invariants</name>
  <read_first>
    @.planning/phases/02-evidence-traceability-mapping/02-CONTEXT.md
    @crates/domain/src/audit_artifacts.rs
  </read_first>
  <files>crates/domain/src/traceability.rs, crates/domain/src/lib.rs</files>
  <behavior>
    - Test 1: rejects links with empty rationale or missing anchor fields.
    - Test 2: accepts `high|medium|low` confidence only and rejects unknown values.
    - Test 3: supports many-to-many links and explicit outcomes for ambiguous, missing, and stale evidence.
  </behavior>
  <action>Create `TraceabilityLink`, `EvidenceAnchor`, `EvidenceType`, `LinkConfidence`, and `LinkOutcome` contracts with validators per D-04, D-07, D-08, D-09, D-10, D-11, and D-12. Require `canonical_requirement_id` and at least one code evidence anchor per requirement mapping (D-03), while allowing optional additional code/test links (D-07). Export the module from `crates/domain/src/lib.rs`.</action>
  <verify>
    <automated>cargo test -p domain traceability::tests::</automated>
  </verify>
  <acceptance_criteria>Domain contract tests prove invalid rationale/confidence/anchor payloads fail closed and valid many-to-many link payloads pass deterministically.</acceptance_criteria>
  <done>`crates/domain/src/traceability.rs` exists with validated types and tests covering D-03, D-04, D-07, D-08, D-09, D-10, D-11, D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add traceability persistence schema with explicit audit outcomes</name>
  <read_first>
    @crates/persistence/migrations/20260409000100_canonical_artifact_ingestion.sql
    @.planning/phases/02-evidence-traceability-mapping/02-CONTEXT.md
  </read_first>
  <files>crates/persistence/migrations/20260409000300_traceability_mapping.sql</files>
  <behavior>
    - Test 1: schema enforces non-empty rationale and confidence enum values.
    - Test 2: schema allows multiple evidence links per requirement without deduping away ambiguity.
    - Test 3: schema can record explicit missing-evidence outcomes.
  </behavior>
  <action>Create migration tables/indexes for traceability snapshot + links + anchor metadata, including constraints that enforce rationale required (D-09), anchor requirements (D-04), confidence enum (`high|medium|low`, D-08), many-to-many mapping support (D-07), and explicit missing/stale outcomes (D-11, D-12). Keep scope strictly traceability mapping; do not add coverage classification/risk columns.</action>
  <verify>
    <automated>cargo test -p persistence postgres::traceability::tests::migration_contract_</automated>
  </verify>
  <acceptance_criteria>Migration contract tests assert required constraints/indexes exist and reject malformed traceability rows.</acceptance_criteria>
  <done>Migration introduces traceability tables and constraints aligned to D-04, D-07, D-08, D-09, D-11, and D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Postgres traceability adapter with deterministic retrieval</name>
  <read_first>
    @crates/persistence/src/postgres/canonical_artifacts.rs
    @crates/domain/src/traceability.rs
  </read_first>
  <files>crates/persistence/src/postgres/traceability.rs, crates/persistence/src/postgres/mod.rs</files>
  <behavior>
    - Test 1: adapter persists code/test evidence anchors with typed error mapping.
    - Test 2: list/read queries return stable ordering for replay determinism.
    - Test 3: constraint violations map to machine-readable error codes.
  </behavior>
  <action>Add SQLx adapter functions to insert and query traceability links with bind parameters only (D-05 deterministic-first foundation). Include typed errors mirroring existing machine-readable conventions and deterministic `ORDER BY` behavior for replay stability. Export module in `postgres/mod.rs` and include tests for constraint violations, ordering, and missing-evidence records (D-06, D-10, D-11).</action>
  <verify>
    <automated>cargo test -p persistence postgres::traceability::tests::</automated>
  </verify>
  <acceptance_criteria>Adapter tests pass for valid writes, constraint failures, ambiguity-preserving reads, and deterministic ordering.</acceptance_criteria>
  <done>`traceability.rs` adapter exposes insert/list functions with machine-readable errors and deterministic read order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service→persistence | Untrusted traceability candidate data crosses into SQL write path |
| repo artifact refs→stored anchors | File/symbol/line references may be stale or malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `crates/persistence/src/postgres/traceability.rs` | mitigate | Use SQLx bind parameters only; add tests asserting no string-concatenated SQL paths. |
| T-02-02 | R | `crates/persistence/migrations/20260409000300_traceability_mapping.sql` | mitigate | Enforce insert-only traceability snapshots and immutable link metadata columns with constraints/trigger checks. |
| T-02-03 | I | `crates/domain/src/traceability.rs` | mitigate | Validate anchor fields and require explicit stale/missing outcomes instead of dropping unresolved entries. |
</threat_model>

<verification>
- `cargo test -p domain traceability::tests::`
- `cargo test -p persistence postgres::traceability::tests::`
</verification>

<success_criteria>
- Traceability link contracts are validated fail-closed and exported for downstream service use.
- Postgres schema/adapter can persist and read deterministic many-to-many requirement→evidence links.
- Ambiguous, missing, and stale evidence states are modeled explicitly with machine-readable metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/02-evidence-traceability-mapping/02-evidence-traceability-mapping-01-SUMMARY.md`
</output>
