---
phase: 02-evidence-traceability-mapping
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - services/research-gateway/src/main.rs
  - services/research-gateway/src/traceability/service.rs
  - crates/domain/src/traceability.rs
  - crates/persistence/src/postgres/traceability.rs
  - tests/api/phase-2-traceability.test.mjs
autonomous: true
gap_closure: true
requirements: [TRAC-01, TRAC-03]
must_haves:
  truths:
    - "Mapped (non-missing) outcomes only use real implementation artifacts as code evidence and always include at least one code anchor."
    - "Traceability links are validated before persistence and snapshot/link/anchor writes succeed atomically with deferred constraints."
  artifacts:
    - path: "services/research-gateway/src/main.rs"
      provides: "Implementation-only evidence candidate filtering (no markdown/docs as code evidence)"
    - path: "services/research-gateway/src/traceability/service.rs"
      provides: "Validated link construction path before persistence"
    - path: "crates/persistence/src/postgres/traceability.rs"
      provides: "Transactional traceability snapshot persistence with deferred-trigger-safe ordering and reason_code persistence"
    - path: "tests/api/phase-2-traceability.test.mjs"
      provides: "Contract assertions for non-empty code anchors on mapped outcomes"
  key_links:
    - from: "services/research-gateway/src/main.rs"
      to: "services/research-gateway/src/traceability/service.rs"
      via: "candidate construction + mapping input assembly"
      pattern: "is_supported_source_file|candidate_from_file"
    - from: "services/research-gateway/src/traceability/service.rs"
      to: "crates/domain/src/traceability.rs"
      via: "build_traceability_link validator"
      pattern: "build_traceability_link"
    - from: "services/research-gateway/src/traceability/service.rs"
      to: "crates/persistence/src/postgres/traceability.rs"
      via: "persist_traceability -> insert_traceability_snapshot transaction"
      pattern: "insert_traceability_snapshot"
---

<objective>
Close Phase 02 verification blockers by tightening evidence qualification and making persistence validation/transactions fail-closed.

Purpose: Restore TRAC-01 and persistence reliability guarantees required by verification.
Output: Candidate filtering + mapped-anchor enforcement + validated transactional persistence.
</objective>

<context>
@.planning/phases/02-evidence-traceability-mapping/02-CONTEXT.md
@.planning/phases/02-evidence-traceability-mapping/02-VERIFICATION.md
@services/research-gateway/src/main.rs
@services/research-gateway/src/traceability/service.rs
@crates/domain/src/traceability.rs
@crates/persistence/src/postgres/traceability.rs
@tests/api/phase-2-traceability.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restrict code evidence to implementation artifacts and enforce mapped code anchors</name>
  <read_first>
    @.planning/phases/02-evidence-traceability-mapping/02-VERIFICATION.md
    @services/research-gateway/src/main.rs
    @tests/api/phase-2-traceability.test.mjs
  </read_first>
  <files>
    services/research-gateway/src/main.rs
    tests/api/phase-2-traceability.test.mjs
  </files>
  <action>
    Update candidate qualification so markdown/docs are excluded from code evidence candidates (per D-01). Keep test evidence detection intact (D-02). Add API contract assertions that rows with outcome != `missing_evidence` must have `code_anchors.length > 0` (per D-03, D-04, D-11).
  </action>
  <verify>
    <automated>node --test tests/api/phase-2-traceability.test.mjs -t "TRAC-01 and TRAC-03"</automated>
  </verify>
  <acceptance_criteria>
    `trace-evidence` no longer classifies `.md` files as code evidence, and mapped rows fail tests if code_anchors is empty.
  </acceptance_criteria>
  <done>Code evidence candidates exclude markdown/docs and mapped outputs enforce non-empty code anchors for non-missing outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce validated link construction and preserve reason_code through persistence</name>
  <read_first>
    @services/research-gateway/src/traceability/service.rs
    @crates/domain/src/traceability.rs
    @crates/persistence/src/postgres/traceability.rs
  </read_first>
  <files>
    services/research-gateway/src/traceability/service.rs
    crates/domain/src/traceability.rs
    crates/persistence/src/postgres/traceability.rs
  </files>
  <action>
    Replace direct `TraceabilityLink` struct construction with domain validator usage (`build_traceability_link`) before persistence (per D-04, D-09). Extend persistence write path so `reason_code` from mapping rows is actually bound and stored instead of forced `None`, preserving explainability for TRAC-03.
  </action>
  <verify>
    <automated>cargo test -p research-gateway traceability::service::tests:: && cargo test -p persistence postgres::traceability::tests::</automated>
  </verify>
  <acceptance_criteria>
    Invalid links are rejected before DB writes, and persisted link records round-trip non-null reason_code values when provided.
  </acceptance_criteria>
  <done>All persisted links are validator-approved and reason_code values are preserved end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make traceability snapshot writes transactionally safe with deferred constraints</name>
  <read_first>
    @crates/persistence/migrations/20260409000300_traceability_mapping.sql
    @crates/persistence/src/postgres/traceability.rs
    @.planning/phases/02-evidence-traceability-mapping/02-REVIEW.md
  </read_first>
  <files>
    crates/persistence/src/postgres/traceability.rs
  </files>
  <action>
    Wrap snapshot/link/anchor inserts inside a single SQL transaction so deferred code-anchor constraint triggers evaluate after anchors exist (per D-03, D-05). Add/adjust adapter tests that fail without transaction and pass with transactional ordering. Also replace unchecked i64→u32 casts with checked conversion to fail closed on invalid anchor lines.
  </action>
  <verify>
    <automated>cargo test -p persistence postgres::traceability::tests::insert_traceability_snapshot_ && cargo test -p persistence postgres::traceability::tests::decode_anchor_</automated>
  </verify>
  <acceptance_criteria>
    Valid link+anchor batches commit successfully under deferred constraints; invalid line values return decode errors (no wrapping cast corruption).
  </acceptance_criteria>
  <done>Traceability snapshot writes are atomic under deferred constraints and anchor decode paths fail closed on invalid values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo scan → mapping candidates | Untrusted repository files become evidence candidates |
| service → persistence | Validated link payload crosses into SQL writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-GC-01 | T | `services/research-gateway/src/main.rs` | mitigate | Block `.md` and non-implementation artifacts from code evidence candidate set. |
| T-02-GC-02 | R | `services/research-gateway/src/traceability/service.rs` | mitigate | Enforce validator path and persist reason_code for complete audit provenance. |
| T-02-GC-03 | D | `crates/persistence/src/postgres/traceability.rs` | mitigate | Execute snapshot/link/anchor writes in one DB transaction under deferred constraints. |
</threat_model>

<verification>
- `cargo test -p research-gateway traceability::service::tests::`
- `cargo test -p persistence postgres::traceability::tests::`
- `node --test tests/api/phase-2-traceability.test.mjs`
</verification>

<success_criteria>
- TRAC-01 blocker closed: code evidence excludes markdown/docs and mapped outcomes require non-empty code anchors.
- Persistence blocker closed: validator path is enforced before writes and inserts are transactional with deferred constraints.
- Explainability preserved: reason_code is persisted and retrievable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-evidence-traceability-mapping/02-evidence-traceability-mapping-03-SUMMARY.md`
</output>
