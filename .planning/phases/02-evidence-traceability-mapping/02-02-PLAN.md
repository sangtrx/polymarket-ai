---
phase: 02-evidence-traceability-mapping
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - services/research-gateway/src/traceability/mod.rs
  - services/research-gateway/src/traceability/matcher.rs
  - services/research-gateway/src/traceability/service.rs
  - services/research-gateway/src/lib.rs
  - services/research-gateway/src/main.rs
  - tests/api/phase-2-traceability.test.mjs
  - tests/e2e/phase-2-traceability-mapping.e2e.test.mjs
  - package.json
autonomous: true
requirements: [TRAC-01, TRAC-02, TRAC-03]
must_haves:
  truths:
    - "User can run traceability mapping and receive one-or-many code evidence links per canonical requirement."
    - "User can see related test evidence links when automated assertions exist."
    - "Every emitted link includes rationale, confidence, and provenance, with explicit missing/ambiguous/stale outcomes."
  artifacts:
    - path: "services/research-gateway/src/traceability/service.rs"
      provides: "Deterministic-first traceability orchestration and fallback handling"
      exports: ["run_traceability_mapping"]
    - path: "services/research-gateway/src/main.rs"
      provides: "Executable CLI entry point for phase-2 mapping output"
      contains: "trace-evidence"
    - path: "tests/api/phase-2-traceability.test.mjs"
      provides: "API/CLI contract checks for TRAC-01..TRAC-03"
      min_lines: 80
    - path: "tests/e2e/phase-2-traceability-mapping.e2e.test.mjs"
      provides: "Deterministic replay and stale/ambiguity evidence assertions"
      min_lines: 80
  key_links:
    - from: "services/research-gateway/src/traceability/service.rs"
      to: "crates/persistence/src/postgres/traceability.rs"
      via: "traceability persistence port calls"
      pattern: "persist_traceability"
    - from: "services/research-gateway/src/main.rs"
      to: "services/research-gateway/src/traceability/service.rs"
      via: "CLI command dispatch"
      pattern: "run_traceability_mapping"
    - from: "tests/api/phase-2-traceability.test.mjs"
      to: "research-gateway trace-evidence CLI"
      via: "spawnSync cargo run command"
      pattern: "trace-evidence"
---

<objective>
Deliver the executable traceability mapping pipeline and verification suite for Phase 2.

Purpose: Produce concrete, deterministic requirement→evidence outputs users can inspect and trust.
Output: Traceability matcher/service, CLI command, and automated API/E2E tests proving TRAC-01..TRAC-03.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-evidence-traceability-mapping/02-CONTEXT.md
@.planning/phases/02-evidence-traceability-mapping/02-RESEARCH.md
@.planning/phases/01-canonical-artifact-ingestion/01-canonical-artifact-ingestion-03-SUMMARY.md
@services/research-gateway/src/ingestion/service.rs
@services/research-gateway/src/main.rs

<interfaces>
From `services/research-gateway/src/ingestion/service.rs`:
```rust
pub async fn run_canonical_ingestion(
    input: RunCanonicalIngestionInput,
) -> Result<CanonicalIngestionResult, CanonicalIngestionError>;
```

From `services/research-gateway/src/main.rs`:
```rust
fn parse_cli_command(args: &[String]) -> Result<CliCommand, CliErrorPayload>;
async fn run_cli(args: &[String]) -> Result<String, CliErrorPayload>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic-first traceability matcher and service</name>
  <read_first>
    @services/research-gateway/src/ingestion/service.rs
    @.planning/phases/02-evidence-traceability-mapping/02-CONTEXT.md
    @crates/domain/src/traceability.rs
  </read_first>
  <files>services/research-gateway/src/traceability/mod.rs, services/research-gateway/src/traceability/matcher.rs, services/research-gateway/src/traceability/service.rs, services/research-gateway/src/lib.rs</files>
  <behavior>
    - Test 1: deterministic anchor matching is preferred whenever canonical requirement IDs are directly referenced.
    - Test 2: semantic fallback emits downgraded confidence and explicit reason code when deterministic anchors are unavailable.
    - Test 3: ambiguous candidates are retained/ranked and missing evidence emits explicit outcome rows.
  </behavior>
  <action>Create `traceability` module implementing mapping orchestration per D-01..D-12: direct implementation artifacts for code evidence (D-01), explicit assertion-backed test evidence links only (D-02), at least one high-quality code reference per mapped requirement (D-03), deterministic-first linking (D-05), fallback with machine-readable reason (D-06), many-to-many support (D-07), confidence scoring (D-08), mandatory rationale (D-09), retain/rank ambiguous candidates (D-10), explicit missing outcomes (D-11), and stale-link invalidation with preserved history metadata (D-12). Keep scope strictly traceability mapping only.</action>
  <verify>
    <automated>cargo test -p research-gateway traceability::tests::</automated>
  </verify>
  <acceptance_criteria>Service tests prove deterministic behavior, explicit fallback metadata, ambiguity preservation, missing outcomes, and stale-link invalidation.</acceptance_criteria>
  <done>`services/research-gateway/src/traceability/` exists and exposes `run_traceability_mapping` with behavior aligned to D-01..D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire traceability CLI command with machine-readable output</name>
  <read_first>
    @services/research-gateway/src/main.rs
    @services/research-gateway/src/traceability/service.rs
  </read_first>
  <files>services/research-gateway/src/main.rs</files>
  <behavior>
    - Test 1: `trace-evidence` command requires commit SHA/timestamp/repo root inputs.
    - Test 2: successful run returns JSON payload containing code/test links plus rationale/confidence fields.
    - Test 3: invalid payload returns machine-readable error code without success-shaped fallback.
  </behavior>
  <action>Extend CLI parser/runner to support `trace-evidence` command that executes the new service and emits deterministic JSON. Reuse fail-closed error payload shape and include required fields for TRAC-01..TRAC-03 (`canonical_requirement_id`, evidence anchors, rationale, confidence, reason codes). Preserve existing `ingest-artifacts` behavior unchanged.</action>
  <verify>
    <automated>cargo test -p research-gateway main::tests::trace_evidence_</automated>
  </verify>
  <acceptance_criteria>CLI tests pass for required flags, success payload contract, and machine-readable failure behavior.</acceptance_criteria>
  <done>`research-gateway` binary supports `trace-evidence` command and emits deterministic JSON links with rationale/confidence metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add phase-2 API/E2E verification suites and QA script</name>
  <read_first>
    @tests/api/phase-1-ingestion.test.mjs
    @tests/e2e/phase-1-canonical-ingestion.e2e.test.mjs
    @package.json
  </read_first>
  <files>tests/api/phase-2-traceability.test.mjs, tests/e2e/phase-2-traceability-mapping.e2e.test.mjs, package.json</files>
  <behavior>
    - Test 1: API contract validates each requirement row includes code evidence and rationale/confidence fields.
    - Test 2: API contract validates test evidence links appear when explicit automated assertions are detected.
    - Test 3: E2E replay validates deterministic ordering and explicit missing/ambiguous/stale outcomes.
  </behavior>
  <action>Create phase-specific Node suites mirroring Phase 1 style to verify TRAC-01..TRAC-03 end-to-end. Include assertions for D-02 automated-test linkage, D-06 fallback reason codes, D-10 ambiguity retention, D-11 missing evidence visibility, and D-12 stale evidence invalidation. Add `qa:test:phase-2` script in `package.json` that runs Rust traceability tests plus the new API/E2E files.</action>
  <verify>
    <automated>npm run qa:test:phase-2</automated>
  </verify>
  <acceptance_criteria>Phase-2 QA command passes and fails if rationale/confidence fields, test evidence links, or explicit unresolved outcomes regress.</acceptance_criteria>
  <done>Phase-2 verification is runnable through one command and covers TRAC-01, TRAC-02, and TRAC-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo artifacts→matcher | Untrusted source text/anchors used to infer evidence links |
| CLI output→downstream audit consumers | Consumers trust output determinism and metadata completeness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `services/research-gateway/src/traceability/service.rs` | mitigate | Deterministic-first matching with explicit fallback reason codes and confidence downgrade. |
| T-02-05 | R | `services/research-gateway/src/main.rs` | mitigate | Emit machine-readable outputs including rationale/confidence/provenance and reject invalid inputs fail-closed. |
| T-02-06 | I | `services/research-gateway/src/traceability/matcher.rs` | mitigate | Restrict code evidence to direct implementation artifacts and validate stale/missing links are explicit (no silent omission). |
</threat_model>

<verification>
- `cargo test -p research-gateway traceability::tests::`
- `cargo test -p research-gateway main::tests::trace_evidence_`
- `node --test tests/api/phase-2-traceability.test.mjs tests/e2e/phase-2-traceability-mapping.e2e.test.mjs`
- `npm run qa:test:phase-2`
</verification>

<success_criteria>
- `trace-evidence` command produces deterministic requirement→code/test links with rationale/confidence for every link.
- Missing, ambiguous, and stale evidence are explicit in outputs with machine-readable reason codes.
- Automated phase-2 suite verifies TRAC-01..TRAC-03 without introducing coverage classification or risk prioritization features.
</success_criteria>

<output>
After completion, create `.planning/phases/02-evidence-traceability-mapping/02-evidence-traceability-mapping-02-SUMMARY.md`
</output>
